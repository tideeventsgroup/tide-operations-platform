// Placeholder — regenerate after each migration with the Supabase MCP
// `generate_typescript_types` tool (project: tide-operations-system,
// xieckrbngacdxcytsoom) or `supabase gen types typescript`.
export type Database = Record<string, never>;
